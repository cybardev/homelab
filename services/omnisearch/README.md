## OmniSearch

Before running the compose file, ensure the source repo is available where expected by running the following command in the same directory as this README file, compose file, and `config.ini`:
```sh
git clone https://git.bwaaa.monster/omnisearch source.vol
```
