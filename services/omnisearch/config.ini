[server]
host = 0.0.0.0
port = 5000
domain = https://search.cybar.dev

[engines]
# Use * for all engines, or specify comma-separated list (e.g., ddg,yahoo)
# Use *,-engine to exclude specific engines (e.g., *,-startpage)
# Available engines: ddg, startpage, yahoo, mojeek
engines = "ddg,yahoo"

[rate_limit]
search_requests = 5
search_interval = 60
images_requests = 5
images_interval = 60
