# ~/.ashrc: interactive shell configuration for BusyBox Ash

# Custom Aliases
alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias recompose='doas nerdctl compose down && doas nerdctl compose up -d'
alias docker='doas nerdctl'
alias sudo='doas'
alias clr='clear'
alias x='exit'
